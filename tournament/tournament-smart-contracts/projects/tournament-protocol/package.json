{
  "name": "exchange-protocol",
  "description": "Exchange contracts from Uniswap v2, and new features (zaps)",
  "version": "2.0.0",
  "license": "GPL-3.0/MIT",
  "private": true,
  "scripts": {
    "start": "npx ts-node testtest.ts",
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run --network testnet scripts/tournament-test.ts",
    "deploy:testluckytour": "npx hardhat run --network testnet scripts/luckytournament-test.ts",
    "deploy:mainnet": "npx hardhat run --network mainnet scripts/deploy.ts",
    "test": "npx hardhat test",
    "deploy:test-contract": "npx hardhat run --network testnet scripts/test-contract.ts",
    "test:coverage": "npx hardhat coverage",
    "test:size-contracts": "npx hardhat size-contracts"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.2.0",
    "@uniswap/lib": "4.0.1-alpha",
    "hardhat": "^2.12.7",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5",
    "web3": "^1.8.1"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.2",
    "@nomiclabs/hardhat-truffle5": "^2.0.7",
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "hardhat-abi-exporter": "^2.10.1",
    "hardhat-contract-sizer": "^2.8.0",
    "solidity-coverage": "^0.8.2",
    "web3": "^1.8.1"
  }
}
